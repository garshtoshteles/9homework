// TODO: Write code to define and export the Employee class
function Employee(role, name, email) {
  this.role = role;
  this.name = name;
  this.email = email;
}

//MUST WRITE getRole FUNCTION that returns role

module.exports = Employee;
